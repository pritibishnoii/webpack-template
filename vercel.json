{
  "routes": [{ "src": "/(.*)", "dest": "/index.html" }],
  "builds": [
    {
      "src": "webpack.config.js",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ]
}
