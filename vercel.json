{
  "version": 2,
  "build": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [{ "src": "/[^,]+", "dest": "/", "status": 200 }]
}
